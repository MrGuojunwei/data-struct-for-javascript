The encryption algorithm and the hash function are further grouped into CipherSpecs The path is based on the object name's hash and a timestamp You can delete or modify hash entries add a new entry and so on